# Compiled binaries and local dev state
chain/build/
.agenticd/

# Go module download cache (only if vendoring locally)
chain/vendor/

# Local secret material — keys must never be committed
*.pem
*.key
*.kdb
keyring-*/

# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/
